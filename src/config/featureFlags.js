// No environment variables - hardcoded for now
const featureFlags = {
  impactScanner: true,      // Set to false to disable
  executiveTemplate: false,
  aiCrystallize: false,
  professionalProfileView: false,
};

export default featureFlags;
